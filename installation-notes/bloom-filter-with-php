https://github.com/mrspartak/php.bloom.filter


just download the file bloom.class.php
then include it in the file where you want to use it with

it uses a hash function to hash all the data and there a check for not including the data shoud be made to get 100  acuurate results

it sets data to memory and hence no need to load data every time

It does not have any self caching capacity so in order to do so use files
